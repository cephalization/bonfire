# Production overrides
# Usage: docker compose -f docker/docker-compose.yml -f docker/docker-compose.prod.yml up

services:
  api:
    environment:
      BONFIRE_API_KEY: ${BONFIRE_API_KEY:?set BONFIRE_API_KEY (openssl rand -base64 32)}
