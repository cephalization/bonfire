# Copy this file to `.env` (repo root).
# Docker Compose automatically reads `.env` for variable substitution.

# --- Better Auth ---

# Required. Use a long random string (32+ chars; ideally 64+).
# Example (Linux): `openssl rand -base64 48`
BETTER_AUTH_SECRET=

# Required. The public base URL used by Better Auth.
#
# Dev (recommended): Better Auth runs on the API origin
# BETTER_AUTH_URL=http://localhost:3000
#
# Prod: use your public HTTPS origin (typically the web UI origin)
# BETTER_AUTH_URL=https://your-hostname
BETTER_AUTH_URL=

# --- Initial admin user (optional) ---
# If set, the API will seed an initial admin user on startup.
INITIAL_ADMIN_EMAIL=admin@example.com
INITIAL_ADMIN_PASSWORD=
INITIAL_ADMIN_NAME=Admin
